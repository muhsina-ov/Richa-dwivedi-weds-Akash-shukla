{
  "name": "cinematic-wedding-invitation",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host --port 3000",
    "build": "vite build",
    "preview": "vite preview --host --port 4173",
    "cf:build": "vite build",
    "cf:preview": "vite preview --host --port 4173",
    "deploy": "vite build && wrangler deploy",
    "deploy:dry": "vite build && wrangler deploy --dry-run",
    "cf:typegen": "wrangler types",
    "optimize-images": "node scripts/optimize-images.mjs",
    "qa:copy": "node scripts/qa-copy.mjs"
  },
  "devDependencies": {
    "ffmpeg-static": "^5.3.0",
    "sharp": "^0.35.3",
    "vite": "^5.4.0",
    "wrangler": "^3.90.0"
  }
}
